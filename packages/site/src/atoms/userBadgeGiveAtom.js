import { atom } from 'recoil'
import { recoilPersist } from 'recoil-persist'

export const userpubbadgegivestate = atom({
  key: 'userPubBadgeGiveAtom',
  default: null,
})

// export const userpasswordstate = atom({
//   key: 'userPasswordAtom',
//   default: null,
// })
